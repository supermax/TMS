using System;using UnityEngine;using TMS.Common.Tasks.Threading;using System.Threading;using NUnit.Framework;namespace TMS.Common.Test.Tasks{	[TestFixture]	public class ThreadHelperTest	{		[Test]		public void TestCreate()		{			Debug.LogFormat("ManagedThreadId: {0}", Thread.CurrentThread.ManagedThreadId);			var t1 = ThreadHelper.CreateThread((Action) AsyncLoop);			Assert.IsNotNull(t1);						t1.Start();		}		private void AsyncLoop()		{			Debug.LogFormat("ManagedThreadId: {0}", Thread.CurrentThread.ManagedThreadId);		}	}}